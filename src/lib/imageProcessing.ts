export type ProcessedImage={blob:Blob;width:number;height:number;sourceWidth:number;sourceHeight:number};
export async function processVehicleImage(file:File,maxLongEdge=3840,quality=.9):Promise<ProcessedImage>{const bitmap=await createImageBitmap(file);const sourceWidth=bitmap.width,sourceHeight=bitmap.height;const long=Math.max(sourceWidth,sourceHeight);const scale=long>maxLongEdge?maxLongEdge/long:1;const width=Math.max(1,Math.round(sourceWidth*scale)),height=Math.max(1,Math.round(sourceHeight*scale));const canvas=document.createElement("canvas");canvas.width=width;canvas.height=height;const ctx=canvas.getContext("2d");if(!ctx)throw new Error("Image processing unavailable");ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality="high";ctx.drawImage(bitmap,0,0,width,height);bitmap.close();const blob=await new Promise<Blob>((resolve,reject)=>canvas.toBlob(b=>b?resolve(b):reject(new Error("Unable to process image")),"image/jpeg",quality));return{blob,width,height,sourceWidth,sourceHeight}}
export function resolutionLabel(w:number,h:number){const long=Math.max(w,h);if(long>=3840)return"4K source";if(long>=2560)return"High resolution";if(long>=1920)return"Full HD";return"Low resolution"}
