"use client";import{FormEvent,useEffect,useState}from"react";import{PageHeader}from"@/components/PageHeader";import{Facebook,Instagram,Star,Megaphone,CheckCircle2,ExternalLink}from"lucide-react";import{useAuth}from"@/providers/AuthProvider";type V={id:string;registration_number:string;make:string;model:string;year?:number;kilometers?:number;target_sale_price?:number;costing_status?:string;status?:string};type Social={instagram_url?:string;facebook_url?:string;google_rating_url?:string};export function Marketing(){const{user}=useAuth(),[social,setSocial]=useState<Social>({}),[vehicles,setVehicles]=useState<V[]>([]),[vehicleId,setVehicleId]=useState(""),[caption,setCaption]=useState(""),[msg,setMsg]=useState("");async function load(){const[a,b]=await Promise.all([fetch("/api/admin-control",{cache:"no-store"}),fetch("/api/vehicles",{cache:"no-store"})]);if(a.ok){const d=await a.json();setSocial(d.social||{})}if(b.ok){const d=await b.json();setVehicles((Array.isArray(d)?d:d.vehicles||[]).filter((v:V)=>v.costing_status==="approved"&&v.status!=="sold"))}}useEffect(()=>{load()},[]);function choose(id:string){setVehicleId(id);const v=vehicles.find(x=>x.id===id);if(v)setCaption(`🚘 ${v.year||""} ${v.make} ${v.model}\n📍 ${Number(v.kilometers||0).toLocaleString("en-IN")} km driven\n💰 Sale Price: ₹${Number(v.target_sale_price||0).toLocaleString("en-IN")}\n\nDrive your dream with LRS Motors. Contact us for details and test drive.`)}async function post(e:FormEvent){e.preventDefault();if(!vehicleId)return setMsg("Select an approved inventory vehicle.");const platforms=[social.instagram_url&&"instagram",social.facebook_url&&"facebook"].filter(Boolean);const r=await fetch("/api/social-campaigns",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({campaignType:"inventory",vehicleId,caption,platforms,createdBy:user?.name})}),d=await r.json();setMsg(r.ok?`Campaign queued for: ${(d.queuedPlatforms||[]).join(", ")||"no connected publishing platforms"}.`:d.error||"Unable to create campaign")}return <div className="min-h-screen bg-[#050708] text-zinc-100"><PageHeader title="Marketing" description="Company social pages and approved inventory campaigns"/><main className="mx-auto max-w-7xl space-y-5 p-4 sm:p-6"><section className="grid gap-3 md:grid-cols-3"><Account title="Instagram Page" icon={<Instagram/>} url={social.instagram_url}/><Account title="Facebook Page" icon={<Facebook/>} url={social.facebook_url}/><Account title="Google Ratings" icon={<Star/>} url={social.google_rating_url}/></section><div className="rounded-xl border border-amber-500/20 bg-amber-500/5 p-3 text-xs text-zinc-400">These company links are controlled by Admin/Director. Sales & Marketing associates can access them here but cannot edit or delete them.</div><section className="rounded-2xl border border-amber-500/20 bg-[#0b1013] p-5"><div className="flex items-center gap-2"><Megaphone className="text-amber-300"/><h2 className="text-xl font-black text-amber-300">CREATE INVENTORY CAMPAIGN</h2></div><p className="mt-1 text-xs text-zinc-500">Uses Director/CEO-approved inventory details and photos captured during vehicle intake.</p><form onSubmit={post} className="mt-5 space-y-4"><label className="block text-xs text-zinc-400">Approved Vehicle<select value={vehicleId} onChange={e=>choose(e.target.value)} className="mt-1 w-full rounded-xl border border-amber-500/20 bg-black/40 p-3"><option value="">Select vehicle</option>{vehicles.map(v=><option key={v.id} value={v.id}>{v.registration_number} — {v.year} {v.make} {v.model} — ₹{Number(v.target_sale_price||0).toLocaleString("en-IN")}</option>)}</select></label><label className="block text-xs text-zinc-400">Post Caption<textarea rows={7} value={caption} onChange={e=>setCaption(e.target.value)} className="mt-1 w-full rounded-xl border border-amber-500/20 bg-black/40 p-3"/></label><div className="rounded-xl border border-white/10 bg-black/20 p-3 text-xs text-zinc-400"><b className="text-amber-300">Publishing plan:</b> approved inventory photos and Make, Model, Registration Year, KM Driven and approved Sale Price are prepared for Instagram and Facebook. Google Ratings opens the company review page configured by Admin.</div><button className="rounded-xl bg-gradient-to-b from-amber-300 to-amber-600 px-6 py-3 font-black text-black">CREATE & QUEUE CAMPAIGN</button></form></section><section className="rounded-2xl border border-amber-500/20 bg-[#0b1013] p-5"><h2 className="font-black text-amber-300">AUTOMATIC DELIVERY POST</h2><p className="mt-2 text-sm text-zinc-400">After a completed sale, the handover/delivery photo can be queued to Instagram and Facebook with the congratulations message after authorized publishing credentials are connected.</p></section>{msg&&<div className="rounded-xl border border-amber-500/20 bg-amber-500/5 p-3 text-sm text-amber-200">{msg}</div>}</main></div>}function Account({title,icon,url}:{title:string;icon:React.ReactNode;url?:string}){const on=Boolean(url);return <div className={`rounded-2xl border p-4 ${on?"border-green-500/35 bg-green-500/[.06]":"border-amber-500/20 bg-[#0b1013]"}`}><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className={on?"text-green-400":"text-zinc-500"}>{icon}</span><div><b>{title}</b><div className={`text-xs ${on?"text-green-400":"text-zinc-500"}`}>{on?"Added by Admin":"Not configured by Admin"}</div></div></div>{on?<a href={url} target="_blank" rel="noreferrer" className="flex items-center gap-1 rounded-lg border border-green-500/30 px-3 py-2 text-xs font-bold text-green-300"><CheckCircle2 className="h-4 w-4"/>OPEN<ExternalLink className="h-3 w-3"/></a>:<span className="text-[10px] text-zinc-600">ADMIN ONLY</span>}</div></div>}