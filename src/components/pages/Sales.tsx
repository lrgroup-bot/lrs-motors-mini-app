"use client";import{useState}from"react";import{Camera,FileText,Search,Send}from"lucide-react";import{useAuth}from"@/providers/AuthProvider";type V={id:string;registration_number:string;make:string;model:string;variant?:string;year?:number;fuel?:string;transmission?:string;kilometers?:number;color?:string;target_sale_price?:number;costing_status:string};type C={id:string;name:string;phone:string;email?:string;address?:string};export function Sales(){const{user}=useAuth(),[reg,setReg]=useState(""),[mobile,setMobile]=useState(""),[vehicle,setVehicle]=useState<V|null>(null),[customer,setCustomer]=useState<C|null>(null),[price,setPrice]=useState(""),[handover,setHandover]=useState(""),[agreement,setAgreement]=useState(""),[msg,setMsg]=useState("");async function fv(){const r=await fetch(`/api/sales-handover?q=${encodeURIComponent(reg)}`),d=await r.json();setVehicle(d);setMsg(d?d.costing_status!=="approved"?"Waiting for Director/CEO costing approval.":"Vehicle ready for sale.":"Vehicle not found.")}async function fc(){const r=await fetch(`/api/customers?search=${encodeURIComponent(mobile)}`),d=await r.json(),x=Array.isArray(d)?d.find((a:C)=>a.phone?.replace(/\D/g,"")===mobile.replace(/\D/g,""))||d[0]:d;setCustomer(x||null);setMsg(x?"Customer found.":"Customer not found in Customer Database.")}async function submit(){if(!vehicle||!customer)return setMsg("Fetch vehicle and customer first.");const r=await fetch("/api/sales-handover",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({vehicleId:vehicle.id,customerId:customer.id,sellingPrice:Number(price),handoverImage:handover,agreementPdf:agreement,soldBy:user?.name})}),d=await r.json();setMsg(r.ok?"Sale completed. Customer communication queued.":d.error||"Unable to submit sale.")}return <div className="min-h-screen bg-[#050708] p-5 text-zinc-100"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-black text-amber-300">SALES & MARKETING</h1><p className="text-xs text-zinc-500">Complete vehicle sale & handover</p></div><a href="/inventory" className="rounded-xl border border-amber-500/40 px-5 py-3 font-bold text-amber-300">SOLD VEHICLES</a></div><div className="mt-5 grid gap-4 xl:grid-cols-3"><Box title="1. REGISTRATION NO."><div className="flex gap-2"><input value={reg} onChange={e=>setReg(e.target.value.toUpperCase())} placeholder="Enter Registration No." className="field"/><button onClick={fv} className="gold"><Search className="h-4 w-4"/>Fetch</button></div>{vehicle&&<div className="mt-4 space-y-2 text-sm"><R a="Registration" b={vehicle.registration_number}/><R a="Make / Model" b={`${vehicle.make} ${vehicle.model}`}/><R a="Variant" b={vehicle.variant||"—"}/><R a="Year / Fuel" b={`${vehicle.year||"—"} / ${vehicle.fuel||"—"}`}/><R a="Transmission" b={vehicle.transmission||"—"}/><R a="KM Driven" b={String(vehicle.kilometers||0)}/><R a="Color" b={vehicle.color||"—"}/>{vehicle.costing_status==="approved"&&<R a="Approved Sale Value" b={`₹${Number(vehicle.target_sale_price||0).toLocaleString("en-IN")}`} gold/>}</div>}</Box><div className="space-y-4"><Box title="2. SELLING PRICE"><input type="number" value={price} onChange={e=>setPrice(e.target.value)} placeholder="Associate enters final selling price" className="field"/></Box><Box title="3. CUSTOMER MOBILE NO."><div className="flex gap-2"><input value={mobile} onChange={e=>setMobile(e.target.value)} placeholder="Customer mobile" className="field"/><button onClick={fc} className="gold">Fetch</button></div>{customer&&<div className="mt-4 space-y-2 text-sm"><R a="Customer" b={customer.name}/><R a="Mobile" b={customer.phone}/><R a="Email" b={customer.email||"—"}/><R a="Address" b={customer.address||"—"}/></div>}</Box></div><Box title="4–5. HANDOVER & AGREEMENT"><Upload icon={<Camera/>} label="Upload / Take Handover Photo" set={setHandover}/><div className="mt-4"><Upload icon={<FileText/>} label="Upload / Take Agreement Photos" set={setAgreement}/></div><p className="mt-4 text-xs text-zinc-500">Agreement images are prepared for PDF processing. After sale, RC + agreement + congratulations communication is queued for WhatsApp/email.</p></Box></div>{msg&&<div className="mt-4 rounded-xl border border-amber-500/20 bg-amber-500/5 p-3 text-sm text-amber-200">{msg}</div>}<button onClick={submit} className="mx-auto mt-5 flex min-w-72 items-center justify-center gap-2 rounded-xl bg-gradient-to-b from-amber-300 to-amber-600 px-8 py-4 font-black text-black"><Send/>SUBMIT SALE</button><style jsx>{`.field{width:100%;border:1px solid rgba(245,158,11,.25);background:#080b0d;border-radius:.7rem;padding:.75rem;color:#fff;outline:none}.gold{display:flex;align-items:center;gap:.35rem;border:1px solid #d99500;border-radius:.7rem;padding:.65rem 1rem;color:#fbbf24;font-weight:800;white-space:nowrap}`}</style></div>}function Box({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-2xl border border-amber-500/20 bg-gradient-to-br from-[#111619] to-[#080b0d] p-5"><h2 className="mb-4 text-sm font-black text-amber-300">{title}</h2>{children}</section>}function R({a,b,gold=false}:{a:string;b:string;gold?:boolean}){return <div className="flex justify-between gap-4 border-b border-white/5 pb-2"><span className="text-zinc-500">{a}</span><b className={gold?"text-amber-300":""}>{b}</b></div>}function Upload({icon,label,set}:{icon:React.ReactNode;label:string;set:(x:string)=>void}){return <label className="block cursor-pointer rounded-xl border border-dashed border-amber-500/40 p-5 text-center text-sm text-zinc-400"><span className="mx-auto mb-2 block w-fit text-amber-300">{icon}</span>{label}<input type="file" accept="image/*,.pdf" capture="environment" className="mt-3 block w-full text-xs" onChange={e=>set(e.target.files?.[0]?.name||"")}/></label>}