"use client";
import {useState}from"react";import{Camera,CheckCircle2,RotateCcw,Image as ImageIcon}from"lucide-react";import{REQUIRED_PHOTOS}from"@/lib/vehicleIntake";
type Item={file?:File;url?:string;tyre?:number;review:"pending"|"approved"|"retake";reason?:string};
export function VehiclePhotoInspection({management=false}:{management?:boolean}){const[items,setItems]=useState<Record<string,Item>>({});function capture(type:string,file?:File){if(!file)return;setItems(x=>({...x,[type]:{...x[type],file,url:URL.createObjectURL(file),review:"pending"}}))}function review(type:string,status:"approved"|"retake"){const reason=status==="retake"?(prompt("Reason for retake (for sales staff):")||"Photo not clear"):undefined;setItems(x=>({...x,[type]:{...x[type],review:status,reason}}))}const completed=REQUIRED_PHOTOS.filter(p=>items[p.type]?.file&&(p.tyre?items[p.type]?.tyre!==undefined:true)).length;return <section className="rounded-2xl border border-white/10 bg-[#0d1011] p-4"><div className="mb-4 flex items-end justify-between"><div><h3 className="font-bold text-amber-300">Mandatory Photo Inspection</h3><p className="text-xs text-zinc-500">Mobile camera capture • {completed}/{REQUIRED_PHOTOS.length} complete</p></div><span className="rounded-full border border-amber-400/30 px-3 py-1 text-xs text-amber-300">All required</span></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{REQUIRED_PHOTOS.map(p=>{const item=items[p.type];return <div key={p.type} className={`overflow-hidden rounded-xl border ${item?.review==="retake"?"border-red-500/50":item?.review==="approved"?"border-green-500/40":"border-white/10"}`}><div className="relative flex aspect-[4/3] items-center justify-center bg-black/30">{item?.url?<img src={item.url} alt={p.label} className="h-full w-full object-cover"/>:<ImageIcon className="h-10 w-10 text-zinc-700"/>}<label className="absolute bottom-2 right-2 cursor-pointer rounded-lg bg-amber-400 px-3 py-2 text-xs font-bold text-black"><Camera className="mr-1 inline h-4 w-4"/>Capture<input type="file" accept="image/*" capture="environment" className="hidden" onChange={e=>capture(p.type,e.target.files?.[0])}/></label></div><div className="p-3"><div className="flex justify-between gap-2"><span className="text-sm font-semibold">{p.label}</span><span className={`text-[10px] uppercase ${item?.review==="approved"?"text-green-400":item?.review==="retake"?"text-red-400":"text-zinc-600"}`}>{item?.review||"required"}</span></div>{p.tyre&&<label className="mt-2 block text-xs text-zinc-500">Tyre remaining %<input type="number" min="0" max="100" value={item?.tyre??""} onChange={e=>setItems(x=>({...x,[p.type]:{...(x[p.type]||{review:"pending"}),tyre:Number(e.target.value)}}))} className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-2 text-white"/></label>}{item?.reason&&<div className="mt-2 rounded-lg bg-red-500/10 p-2 text-xs text-red-300"><RotateCcw className="mr-1 inline h-3 w-3"/>{item.reason}</div>}{management&&item?.file&&<div className="mt-3 grid grid-cols-2 gap-2"><button onClick={()=>review(p.type,"approved")} className="rounded-lg bg-green-500/10 p-2 text-xs text-green-400"><CheckCircle2 className="mr-1 inline h-4 w-4"/>Approve</button><button onClick={()=>review(p.type,"retake")} className="rounded-lg bg-red-500/10 p-2 text-xs text-red-400"><RotateCcw className="mr-1 inline h-4 w-4"/>Retake</button></div>}</div></div>})}</div></section>}
