import{NextResponse}from"next/server";import{parseRcText,verifyRcIdentifiers,type RcFields}from"@/lib/rcParser";
export const runtime="nodejs";export const dynamic="force-dynamic";
function mergeFields(...sets:RcFields[]){const out:RcFields={};for(const set of sets)for(const[k,v]of Object.entries(set))if(v&&!out[k])out[k]=v;return out}
async function extractPdfText(file:File){const pdfjs=await import("pdfjs-dist/legacy/build/pdf.mjs");const bytes=new Uint8Array(await file.arrayBuffer());const pdf=await pdfjs.getDocument({data:bytes,isEvalSupported:false,useWorkerFetch:false}).promise;let text="";for(let n=1;n<=pdf.numPages;n++){const page=await pdf.getPage(n);const content=await page.getTextContent();text+=" "+content.items.map((i:any)=>("str"in i?i.str:"")).join(" ")}return text.trim()}
async function extractImageText(file:File){const{createWorker}=await import("tesseract.js");const worker=await createWorker("eng");try{const result=await worker.recognize(Buffer.from(await file.arrayBuffer()));return result.data.text||""}finally{await worker.terminate()}}
export async function POST(req:Request){try{const form=await req.formData();const file=form.get("file");if(!(file instanceof File))return NextResponse.json({error:"RC file is required"},{status:400});if(file.size>20*1024*1024)return NextResponse.json({error:"RC exceeds 20 MB"},{status:413});const isPdf=file.type==="application/pdf"||file.name.toLowerCase().endsWith(".pdf");const isImage=file.type.startsWith("image/");if(!isPdf&&!isImage)return NextResponse.json({error:"Upload an RC PDF or RC image/photo."},{status:415});let pdfText="",ocrText="";const methods:string[]=[];if(isPdf){pdfText=await extractPdfText(file);if(pdfText){methods.push("pdf-text")}else return NextResponse.json({error:"This RC PDF has no readable embedded text. Capture/upload a clear RC image so OCR can read it."},{status:422})}else{ocrText=await extractImageText(file);if(ocrText.trim())methods.push("image-ocr")}const pdfFields=parseRcText(pdfText);const ocrFields=parseRcText(ocrText);const fields=mergeFields(pdfFields,ocrFields);if(!Object.values(fields).some(Boolean))return NextResponse.json({error:"No RC details could be extracted. Please capture a clearer image or enter the details manually."},{status:422});const verification=verifyRcIdentifiers(fields);return NextResponse.json({fields,verification,methods,rawText:(pdfText||ocrText).slice(0,12000),message:"RC extracted locally. Staff must verify every field before continuing."})}catch(e){console.error("RC extraction error",e);return NextResponse.json({error:e instanceof Error?e.message:"RC extraction failed"},{status:500})}}
