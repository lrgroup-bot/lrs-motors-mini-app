import type{Metadata}from"next";import"./globals.css";import{Navigation}from"@/components/Navigation";import{AuthProvider}from"@/providers/AuthProvider";
export const metadata:Metadata={title:"LRS Motors - PC Server",description:"LRS Motors dealership management server",viewport:"width=device-width, initial-scale=1, maximum-scale=1"};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body className="bg-[#050607]"><AuthProvider><div className="min-h-screen"><Navigation/><main className="min-h-screen w-full pt-20">{children}</main></div></AuthProvider></body></html>}
